#!groovy
properties(
        [[$class: 'GithubProjectProperty', projectUrlStr: 'https://git.reform.hmcts.net/case-management/user-profile-app'],
         pipelineTriggers([[$class: 'GitHubPushTrigger']])]
)

@Library("Infrastructure")

def type = "java"

def product = "ccd"

def component = "user-profile-api"

List<LinkedHashMap<String, Object>> secrets = [
    secret('microserviceDataStoreName', 'CCD_DS_SERVICE_NAME', '4c81f5d18d5947c28bc4116231525fed'),
    secret('microserviceDataStoreSecret', 'CCD_DS_SERVICE_SECRET', '76393374cbcc4c87adf520b89bcd9495'),
]

static LinkedHashMap<String, Object> secret(String secretName, String envVar, String version) {
    [ $class: 'AzureKeyVaultSecret',
      secretType: 'Secret',
      name: secretName,
      version: version,
      envVariable: envVar
    ]
}

withPipeline(type, product, component) {
    loadVaultSecrets(secrets)
}
