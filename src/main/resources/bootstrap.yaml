spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/ccd
      aliases:
        user-profile-api-POSTGRES-USER-V11: USER_PROFILE_DB_USERNAME
        user-profile-api-POSTGRES-PASS-V11: USER_PROFILE_DB_PASSWORD
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
